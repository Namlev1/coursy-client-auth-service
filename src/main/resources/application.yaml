spring:
  application:
    name: users
  datasource:
    url: jdbc:postgresql://localhost:5432/users_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update

#  profiles:
#    active: setup
app:
  setup:
    roles: "STUDENT, TEACHER, SUPER_ADMIN" # Default roles